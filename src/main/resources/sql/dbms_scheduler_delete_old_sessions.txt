BEGIN
    DBMS_SCHEDULER.create_job (
        job_name        => 'DELETE_OLD_SESSIONS',
        job_type        => 'PLSQL_BLOCK',
        job_action      => 'BEGIN delete_old_rows; END;',
        start_date      => SYSTIMESTAMP,
        repeat_interval => 'FREQ=MINUTELY; INTERVAL=1',
        enabled         => TRUE
    );
END;
/